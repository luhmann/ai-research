{
  "newrelic": {
    "api_key": "YOUR_NEW_RELIC_API_KEY",
    "account_id": "YOUR_ACCOUNT_ID",
    "nrql_query": "SELECT * FROM Log WHERE service.name = 'your-service' SINCE 1 minute ago",
    "poll_interval_seconds": 5
  },
  "graphql": {
    "endpoint": "http://localhost:4000/graphql",
    "mutation_template": "mutation CreateEvent($data: EventInput!) { createEvent(data: $data) { id success } }"
  },
  "filters": {
    "message_pattern": ".*",
    "required_fields": ["message", "timestamp"]
  },
  "mapping": {
    "message": "data.message",
    "timestamp": "data.timestamp",
    "level": "data.level",
    "service": "data.service"
  }
}
